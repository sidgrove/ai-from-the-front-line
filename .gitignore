# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor / tooling
.vscode/
*.swp
.claude/
.worktrees/

# Vercel local project link
.vercel/

# Exported deck (rebuild with Print -> Save as PDF)
*.pdf

# Exported deck (rebuild from the deck: PDF via print, PPTX via the export script)
*.pptx
# Office lock files
~$*
